# Root environment variables used by docker-compose for local development.
# Copy this file to .env and adjust values before starting containers.

POSTGRES_DB=gestionale
POSTGRES_USER=app
POSTGRES_PASSWORD=app
